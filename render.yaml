services:
  - type: web
    name: wellnesslog
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: wellnesslog-db
          property: connectionString

databases:
  - name: wellnesslog-db
    plan: free
